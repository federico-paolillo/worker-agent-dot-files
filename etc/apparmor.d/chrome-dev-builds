abi <abi/4.0>,
include <tunables/global>

profile chrome /home/codex/.local/bin/uchromium/152/chrome flags=(unconfined) {
  userns,

  # Site-specific additions and overrides. See local/README for details.
  include if exists <local/chrome>
}
